# incsearch

We can't support `incsearch` because we have progressive loading of commits. If we do implement this one day, it would have to be only work across currently loaded commits. (which could be all if you did a `/term/!` search).